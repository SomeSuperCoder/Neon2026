// Example: Conditionals
// Level: 2
// Features: if-else statements, comparison operators, boolean logic
// Description: Demonstrates conditional branching with if-else statements

export function entry(): i64 {
    let x: i64 = 10;
    if (x > 5) {
        return 1;
    } else {
        return 0;
    }
}
